---
import PublicLayout from "../../layouts/PublicLayout.astro";
import { RegisterForm } from "../../components/auth/RegisterForm";

// This page requires SSR to handle cookies and redirects
export const prerender = false;

// Get the 'next' parameter from URL for redirect after registration
const nextUrl = Astro.url.searchParams.get("next") || "/app/dashboard";
---

<PublicLayout title="Zarejestruj się - SmartWordsAI">
  <RegisterForm nextUrl={nextUrl} client:load />
</PublicLayout>
